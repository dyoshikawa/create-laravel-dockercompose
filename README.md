```
git clone https://github.com/dyoshikawa/create-laravel-dockercompose
mv create-laravel-dockercompose.sh /usr/local/bin/create-laravel-dockercompose
chmod +x /usr/local/bin/create-laravel-dockercompose
```
